### Enhancements

* Add more robust channel name/url comparison in `channel_settings` lookup by using `Channel.canonical_name`, normalizing trailing slashes and handling missing or `None` channel values gracefully (#16510)
