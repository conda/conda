### Enhancements

* Add `conda.core.index.Index` as a faster drop-in replacement of realized dictionary index. (#13880)
  Note: This removes the implicit initialization of the logging from `get_index`. If you relied on that, you should probably call `conda.gateways.logging.initialize_logging` explicitly.

### Bug fixes

* <news item>

### Deprecations

* Mark `conda.core.index.get_index` as pending deprecation. Use `conda.core.index.Index` instead. (#13880)
* Mark `conda.core.index.fetch_index` as pending deprecation. Use `conda.core.index.Index` instead. (#13880)

### Docs

* <news item>

### Other

* <news item>
