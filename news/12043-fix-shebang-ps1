### Enhancements

* <news item>

### Bug fixes

* Prevent `conda` from using `/bin/sh` + `exec` trick for its own entry point,
  which drops `$PS1` on some shells (#11885, #11893 via #12043).

### Deprecations

* <news item>

### Docs

* <news item>

### Other

* <news item>
