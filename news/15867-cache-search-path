### Enhancements

* Cache the result of `Configuration._set_search_path()` so repeated calls with the same arguments (e.g. across multiple `context.__init__()` invocations) skip the filesystem stat and directory-iteration work. The expanded search path is now stored as a `tuple` instead of a lazy generator. (#15867)

### Bug fixes

* <news item>

### Deprecations

* <news item>

### Docs

* <news item>

### Other

* <news item>
