### Enhancements

* Skip the SHA-256 of rewritten files in `PrefixReplaceLinkAction.execute()` when `extra_safety_checks` is disabled (the default). Shaves ~27% off per-file prefix-replacement cost for packages with large binaries (PyTorch, TensorFlow, etc.). (#15972)

### Bug fixes

* <news item>

### Deprecations

* <news item>

### Docs

* <news item>

### Other

* <news item>
