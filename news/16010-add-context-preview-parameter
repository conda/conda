### Enhancements

* Add `context.preview` configuration parameter (via `CONDA_PREVIEW` env var or `preview:` in `.condarc`) and `context.preview_enabled(value)` method for opt-in preview feature checks. (#16010)

### Bug fixes

* <news item>

### Deprecations

* <news item>

### Docs

* <news item>

### Other

* <news item>
