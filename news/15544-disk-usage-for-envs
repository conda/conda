### Enhancements

* Add a `--size` flag to the `conda info --envs` and `conda env list` commands to display disk usage for each environment. (#8040 and #15544)

### Bug fixes

* Ensure `size_in_bytes` is recorded for generated entry points and compiled `.pyc` files in the prefix manifests (#15544).

### Deprecations

* <news item>

### Docs

* <news item>

### Other

* <news item>
