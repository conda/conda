Creates (or updates) repodata.json in channel directories. (ADVANCED)

**usage**: ``conda index [-h] [-f] [-q] [DIRECTORIES [DIRECTORIES ...]]``

*DIRECTORIES*
        optional arguments:

optional arguments:
  -h, --help   show this help message and exit
  -f, --force  force reading all files
  -q, --quiet
