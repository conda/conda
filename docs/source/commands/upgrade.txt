Upgrade Anaconda CE install to full Anaconda trial.

**usage**: ``conda upgrade [-h] [--yes] [--dry-run]``

optional arguments:
  -h, --help  show this help message and exit
  --yes       do not ask for confirmation
  --dry-run   only display what would have been done
