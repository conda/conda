# Copyright (C) 2012 Anaconda, Inc
# SPDX-License-Identifier: BSD-3-Clause

package:
  name: buildstring
  version: 1.0

build:
  number: 0
  string: versioned_{{ versioned }}
  noarch: generic

requirements:
  run:
    - versioned {{ versioned }}

about:
  home: https://github.com/conda/conda
  license: BSD-3-Clause
  summary: A build string package

extra:
  test_purpose: A build string package to test package revisions
