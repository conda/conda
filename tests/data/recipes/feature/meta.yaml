package:
  name: feature
  version: "{{ pkg_version }}"

build:
  noarch: generic
  features:
    - "{{ feature }}"

requirements:
  run:
    - track_feature

about:
  summary: "Package declaring features: {{ feature }}"
