package:
  name: another_dependent
  version: '{{ pkg_version }}'

build:
  noarch: generic
  string: "0"

requirements:
  run:
    - dependent

about:
  summary: Second consumer of dependent for multi-package remove/update graphs
