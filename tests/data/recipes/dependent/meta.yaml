package:
  name: dependent
  version: '{{ pkg_version }}'

build:
  noarch: generic

requirements:
  run:
    - '{{ pin_subpackage("dependency") }}'

outputs:
  - name: dependency
    build:
      noarch: generic

about:
  summary: Depends on co-built dependency output; generic pin/remove fixture
