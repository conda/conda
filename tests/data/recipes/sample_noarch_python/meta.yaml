package:
  name: "sample_noarch_python"
  version: "1.0"

build:
  noarch: python
  script: touch $SP_DIR/sample.py

requirements:
  host:
    - python
  run:
    - python

about:
  summary: "noarch: python package installing into $SP_DIR"
