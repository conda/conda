package:
  name: post_link_run_in_env_package
  version: 1.0

build:
  noarch: generic

about:
  summary: >
    Post-link script that requires an activated env (TEST_VAR set by activate.d);
    fails if the post-link runs outside that env (e.g. base)
