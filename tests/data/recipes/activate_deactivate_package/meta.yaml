package:
  name: activate_deactivate_package
  version: 1.0

source:
  path: ./src

build:
  noarch: generic
  script:
    # since this is a noarch package we don't care that this is unix
    - cp -r etc ${PREFIX}

about:
  summary: Activate/deactivate scripts that set environment variables
