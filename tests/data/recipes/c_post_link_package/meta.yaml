package:
  name: c_post_link_package
  version: 1.0.0

build:
  number: 0
  noarch: generic

requirements:
  run:
    - b_post_link_package

about:
  summary: Post-link script that depends on b_post_link_package (link order)
