package:
  name: clobber-a
  version: 1.0.0

build:
  number: 0
  noarch: generic
  script:
    # since this is a noarch package we don't care that this is unix
    - mkdir -p ${PREFIX}/bin
    - echo "clobber-a" > ${PREFIX}/bin/clobber-test-file


about:
  summary: Test package A for file clobbering regression tests
