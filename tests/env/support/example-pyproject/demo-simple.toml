[tool.conda.environment]
channels = [
  "pytorch",
  "nvidia",
  "defaults",
]
dependencies = [
  "pytorch",
  "pytorch-cuda",
  "torchaudio",
  "torchvision",
  "pip",
  {pip = ["requests"]},
]
name = "demo"
