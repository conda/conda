### Enhancements

* Add `LinkSourceNotFoundError` and `LinkScriptError` (a `LinkError` subclass) so callers can detect a missing package cache source and a failed pre-/post-link script with `isinstance` and inspect `src`, `action`, `prec` and `path` instead of parsing error messages. Error text and exception hierarchy are unchanged.

### Bug fixes

* <news item>

### Deprecations

* <news item>

### Docs

* <news item>

### Other

* Include the new `src` / `action` / `prec` / `path` fields and exception names in `--json` error output.
