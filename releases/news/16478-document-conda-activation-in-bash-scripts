### Docs

* Document how to activate conda environments from shell scripts using `conda shell.<shell> hook`, and mention `conda run` as an alternative for single-command execution. (#7980 via #16478)
